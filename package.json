{
  "name": "book-library",
  "version": "0.0.0",
  "private": true,
  "description": "A simple Node.js Book Library application.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
